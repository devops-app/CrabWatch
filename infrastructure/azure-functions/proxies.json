{
  "$schema": "http://json.schemastore.org/proxies",
  "proxies": {
    "api-proxy": {
      "matchCondition": {
        "route": "/api/{*route}"
      },
      "backendUri": "http://localhost:3001/api/{route}"
    }
  }
}
